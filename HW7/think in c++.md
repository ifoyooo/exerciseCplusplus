- why do we need function pointer?

Just like the function-sort in the <algorithm>library, what you want is sort the list based on comparison, but the the standard about less is dependent on your self.